{
  "components": {
    "requestBodies": {
      "SmsSendRequest": {
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/SendRequest"
            }
          }
        },
        "description": "SmsSendRequest"
      }
    },
    "responses": {
      "MicroAPIError": {
        "content": {
          "application/json": {
            "schema": {
              "properties": {
                "Code": {
                  "description": "Error code",
                  "example": 500,
                  "type": "number"
                },
                "Detail": {
                  "description": "Error detail",
                  "example": "service not found",
                  "type": "string"
                },
                "Id": {
                  "description": "Error ID",
                  "type": "string"
                },
                "Status": {
                  "description": "Error status message",
                  "example": "Internal Server Error",
                  "type": "string"
                }
              },
              "title": "MicroAPIError",
              "type": "object"
            }
          }
        },
        "description": "Error from the Micro API"
      },
      "SmsSendResponse": {
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/SendResponse"
            }
          }
        },
        "description": "SmsSendResponse"
      }
    },
    "schemas": {
      "SendRequest": {
        "description": "Send an SMS.",
        "properties": {
          "from": {
            "description": "who is the message from? The message will be suffixed with \"Sent from \u003cfrom\u003e\"",
            "type": "string"
          },
          "message": {
            "description": "the main body of the message to send",
            "type": "string"
          },
          "to": {
            "description": "the destination phone number including the international dialling code (e.g. +44)",
            "type": "string"
          }
        },
        "title": "SendRequest",
        "type": "object"
      },
      "SendResponse": {
        "properties": {
          "info": {
            "description": "any additional info",
            "type": "string"
          },
          "status": {
            "description": "will return \"ok\" if successful",
            "type": "string"
          }
        },
        "title": "SendResponse",
        "type": "object"
      }
    },
    "securitySchemes": {
      "M3OAPIToken": {
        "bearerFormat": "token",
        "description": "M3O API token",
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "description": "Generated by protoc-gen-openapi",
    "title": "Sms",
    "version": "1",
    "x-logo": {
      "altText": "M3O logo",
      "backgroundColor": "#000000",
      "url": "https://raw.githubusercontent.com/m3o/m3o/main/images/logo.png"
    }
  },
  "openapi": "3.0.0",
  "paths": {
    "/sms/Sms/Send": {
      "parameters": [
        {
          "in": "header",
          "name": "Micro-Namespace",
          "required": true,
          "schema": {
            "default": "micro",
            "type": "string"
          }
        }
      ],
      "post": {
        "requestBody": {
          "$ref": "#/components/requestBodies/SmsSendRequest"
        },
        "responses": {
          "200": {
            "$ref": "#/components/responses/SmsSendResponse"
          },
          "default": {
            "$ref": "#/components/responses/MicroAPIError"
          }
        },
        "security": [
          {
            "MicroAPIToken": []
          }
        ],
        "summary": "Sms.Send(SendRequest)"
      }
    }
  },
  "servers": [
    {
      "url": "https://api.m3o.com/v1",
      "description": "M3O platform"
    }
  ]
}